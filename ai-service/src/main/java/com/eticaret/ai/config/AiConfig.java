package com.eticaret.ai.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AiConfig {
    // Spring AI bağımlılığı kaldırıldı — ChatClient yerine AIService içinde doğrudan WebClient kullanılıyor
}